# Gemini API key — get yours free at https://aistudio.google.com/app/apikey
# Copy this file to .env and replace the placeholder below with your actual key.
# The app works without it (fallback insights are generated locally).
VITE_GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
